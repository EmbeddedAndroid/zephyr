# Override the board default (swap-using-offset) with swap-using-move so the
# standard flash_img DFU path (writes to slot1 base) is compatible and the A/B
# upgrade supports automatic rollback of an unconfirmed image.
CONFIG_BOOT_SWAP_USING_MOVE=y
