# Out-of-tree module providing the charlieplex LED-matrix display driver.
# Mirrors the in-tree drivers/display layout so the source can be lifted into
# mainline Zephyr (drivers/display/) with minimal change when upstreaming.
add_subdirectory(drivers/display)
